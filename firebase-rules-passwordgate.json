{
  "comment": "Use this while the admin is protected only by a shared password (no Firebase Auth). Reads are public; writes are open. Anyone who discovers the database URL could write, so treat this as low-security. Move to firebase-rules-auth.json once you add Firebase Auth.",
  "rules": {
    "cards": {
      ".read": true,
      ".write": true
    }
  }
}
